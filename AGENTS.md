> [!IMPORTANT]
> Avoid rewriting published git history — force pushing, or rebasing/amending/
> squashing commits that are already pushed — as it rewrites history and the
> user will likely lose their project history.
>
> Keep the branch in a working state: commits that push to the connected remote
> end up in the deployed build, so verify changes before pushing.
